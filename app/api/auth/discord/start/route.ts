import {platformEnv,randomId} from "@/lib/platform";
export async function GET(request:Request){const clientId=platformEnv().DISCORD_CLIENT_ID;if(!clientId)return Response.json({error:"Discord ist noch nicht konfiguriert. DISCORD_CLIENT_ID fehlt."},{status:503});const state=randomId(),redirect=`${new URL(request.url).origin}/api/auth/discord/callback`;const target=new URL("https://discord.com/oauth2/authorize");target.search=new URLSearchParams({response_type:"code",client_id:clientId,scope:"identify email connections",state,redirect_uri:redirect,prompt:"consent"}).toString();return new Response(null,{status:302,headers:{Location:target.toString(),"Set-Cookie":`oauth_state=${state}; Path=/api/auth/discord; HttpOnly; SameSite=Lax; Max-Age=600${new URL(request.url).protocol==="https:"?"; Secure":""}`}})}
