"use client";import {useEffect,useState} from "react";export default function Downloads(){const[data,setData]=useState<any>(null),[message,setMessage]=useState("");useEffect(()=>{fetch("/api/v1/resources?vtcId=vtc-ngl").then(r=>r.json()).then(setData)},[]);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=Object.fromEntries(new FormData(e.currentTarget)),r=await fetch("/api/v1/resources",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({action:"download",vtcId:"vtc-ngl",data:f})}),j=await r.json();setMessage(r.ok?(j.pendingApproval?"Download wartet auf Plattformfreigabe.":"Download freigegeben."):j.error)}if(!data)return <main className="resource-loading">Downloads werden geladen …</main>;return <main className="resource-page"><header><div><span>CONVOYHUB · DOWNLOADS</span><h1>Client, Skins & Modpakete</h1></div><nav><a href="/wiki">Anleitungen</a><a href="/dashboard">Dashboard</a></nav></header><section className="version-grid">{data.versions.map((v:any)=><article key={v.id}><small>{v.product} · {v.channel}</small><h2>Version {v.version}</h2><p>{v.release_notes}</p><span>{v.mandatory?"Pflichtupdate":"Optional"} · {JSON.stringify(v.compatibility)}</span>{v.download_url&&<a href={v.download_url}>Herunterladen</a>}</article>)}</section><section className="download-grid">{data.downloads.map((d:any)=><article key={d.id}><small>{d.type} · Version {d.version||"—"}</small><h2>{d.title}</h2><p>{d.description}</p><dl><div><dt>Spielversionen</dt><dd>{d.game_versions.join(", ")||"Alle"}</dd></div><div><dt>DLC</dt><dd>{d.dlc_requirements.join(", ")||"Keine"}</dd></div><div><dt>Abhängigkeiten</dt><dd>{d.dependencies.join(", ")||"Keine"}</dd></div><div><dt>Prüfsumme</dt><dd><code>{d.checksum||"—"}</code></dd></div></dl>{d.external_url&&<a href={`/api/v1/resources?download=${d.id}`}>Download ({d.download_count})</a>}</article>)}</section>{data.canManage&&<form className="resource-form" onSubmit={submit}><h2>Download zur Freigabe einreichen</h2><div><input name="title" placeholder="Titel" required/><select name="type"><option>Desktop-Client</option><option>Firmen-Skin</option><option>LKW-Skin</option><option>Auflieger-Skin</option><option>Modpaket</option><option>Convoy-Modpaket</option></select><input name="version" placeholder="Version"/><input name="externalUrl" type="url" placeholder="Download-URL"/><input name="gameVersions" placeholder="ETS2 1.xx, ATS 1.xx"/><input name="dlcRequirements" placeholder="DLC-Anforderungen"/><input name="dependencies" placeholder="Abhängigkeiten"/><input name="checksum" placeholder="SHA-256-Prüfsumme"/></div><textarea name="description" placeholder="Beschreibung und Installationshinweise"/><button>Zur Freigabe einreichen</button>{message&&<p>{message}</p>}</form>}</main>}
