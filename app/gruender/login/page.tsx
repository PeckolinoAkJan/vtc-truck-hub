"use client";
import {FormEvent,useState} from "react";
export default function FounderLogin(){const[message,setMessage]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setMessage("Anmeldung wird geprüft …");const body=Object.fromEntries(new FormData(e.currentTarget));const res=await fetch("/api/auth/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(body)}),json=await res.json();if(!res.ok)return setMessage(json.error??"Anmeldung fehlgeschlagen");location.href="/gruender"}return <main className="founder-login"><section><a className="brand" href="/"><span className="brand-mark">CH</span><span>CONVOY<span>HUB</span></span></a><span className="kicker">GESCHÜTZTER PLATTFORMBEREICH</span><h1>Gründer-Zentrale</h1><p>Dieser Zugang akzeptiert ausschließlich das dauerhaft hinterlegte Gründerkonto.</p><form onSubmit={submit}><label>E-Mail<input name="email" type="email" required/></label><label>Passwort<input name="password" type="password" required/></label><button className="primary" type="submit">Sicher anmelden</button></form>{message&&<p className="form-message">{message}</p>}<a href="/konto">Normales Fahrerkonto öffnen</a></section></main>}
