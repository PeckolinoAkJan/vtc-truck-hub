const token=process.env.DISCORD_BOT_TOKEN,applicationId=process.env.DISCORD_APPLICATION_ID,guildId=process.env.DISCORD_GUILD_ID;if(!token||!applicationId)throw new Error("DISCORD_BOT_TOKEN und DISCORD_APPLICATION_ID sind erforderlich");const commands=[{name:"status",description:"Zeigt den ConvoyHub-Systemstatus"},{name:"event",description:"Zeigt das nächste Event"},{name:"regeln",description:"Zeigt die hinterlegten Serverregeln"},{name:"ticket",description:"Öffnet ein Support-Ticket"}],path=guildId?`applications/${applicationId}/guilds/${guildId}/commands`:`applications/${applicationId}/commands`,r=await fetch(`https://discord.com/api/v10/${path}`,{method:"PUT",headers:{Authorization:`Bot ${token}`,"Content-Type":"application/json"},body:JSON.stringify(commands)});if(!r.ok)throw new Error(await r.text());console.log(`${commands.length} ConvoyHub-Befehle registriert.`);
